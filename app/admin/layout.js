export const metadata = {
  title: 'Admin - Luxury Times',
  description: 'Admin portal for Luxury Times',
};

export default function AdminLayout({ children }) {
  return (
    <section>
      {children}
    </section>
  );
} 